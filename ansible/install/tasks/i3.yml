---
- name: install flameshot (screen shot tool)
  ignore_errors: true
  become: true
  package:
    name: flameshot
    state: latest

- name: install kitty
  ignore_errors: true
  become: true
  package:
    name: kitty
    state: latest

- name: link i3 config
  ignore_errors: true
  file:
    src: "{{ personal_dir }}/.config/i3"
    dest: "{{ ansible_env.HOME }}/.config/i3"
    state: link
    force: true
